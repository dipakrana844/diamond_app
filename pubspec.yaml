name: practical_app
description: "A new Flutter project."

publish_to: 'Dipak Rana'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  excel: ^4.0.6
  shared_preferences: ^2.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/Flutter-test-dataset.xlsx
